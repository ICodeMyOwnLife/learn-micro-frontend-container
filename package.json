{
  "name": "micro-frontend-container",
  "version": "1.0.0",
  "main": "index.js",
  "author": "ICodeMyOwnLife <icodemyownlife@gmail.com>",
  "license": "MIT",
  "scripts": {
    "build": "yarn --cwd server build && yarn --cwd client build",
    "postinstall": "yarn --cwd client && yarn --cwd server"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  },
  "engines": {
    "node": ">= 12.16.0"
  }
}
