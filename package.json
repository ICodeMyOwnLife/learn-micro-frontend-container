{
  "name": "micro-frontend-container",
  "version": "1.0.0",
  "main": "index.js",
  "author": "Hien CB",
  "license": "MIT",
  "scripts": {
    "build": "yarn --cwd server build && yarn --cwd client build",
    "postinstall": "yarn --cwd client && yarn --cwd server",
    "serve": "NODE_ENV=production node server/build"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  },
  "engines": {
    "node": ">= 12.16.0"
  }
}
